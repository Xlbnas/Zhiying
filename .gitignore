node_modules/
.next/
out/
# 仅忽略仓库根目录的运行时 data/（SQLite、bundle 缓存、渲染输出）；
# 不得吞掉 src/remotion/data（构建必需的渲染数据文件，已入库）
/data/
*.tsbuildinfo
next-env.d.ts
.env
.env.local
.DS_Store

# 455MB 二进制媒体资产（BGM/SFX/TTS/图片）——与 .dockerignore 对齐，
# 部署时随项目目录拷贝（DEPLOY.md §0），不进入 git
public/full/
public/pilot/

# 帧对比工具的独立依赖（scripts/README.md 有使用说明）
scripts/frame-compare/node_modules/

# 临时日志
*.log

# M3-F adapter 本地隔离依赖环境（不入库）
services/indextts2-api-adapter/.venv/

# 所有环境配置文件默认不入 Git（.env 前缀与 *.env 后缀全覆　.env.production、
# .env.production.local、API-KEY.env 等）；只保留无 secret 模板
.env*
*.env
!.env.example

# production runtime/deployment state（根目录锚定；不入库：
# 真实 voice registry、reference voice、admin proposed compose）
/voice-registry.json
/voices/
/_m4c1/
