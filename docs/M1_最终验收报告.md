# 知影 M1 最终验收报告

验收时间：2026-07-21/22 ｜ 方式：基于接管审计的定点修复 + 全链路实机验证 ｜ 未从零重做，未进入 M2

## 一、修复清单（4 项，全部落地）

| # | 问题 | 修复 | 改动 |
|---|---|---|---|
| P0 | `React is not defined`：tsconfig 的 `jsx:'preserve'` 经 esbuild-loader 的 tsconfigRaw 传入 esbuild，classic JSX 输出无 React 导入，bundle 在 headless Chrome 执行即炸 | `src/worker/index.ts` 的 `webpackOverride` 中对 esbuild-loader 强制 `jsx:'automatic'`（已实证 esbuild 两种模式输出差异）；删除旧 bundle-cache 重新打包 | 1 文件 ~40 行 |
| P1 | retry 复用模块级 `RENDER_PORT`，渲染失败后端口未释放，同进程 retry 必撞（实测 `port is not available`） | 端口改为 `randomRenderPort()` 每次 `runJob` 内生成 | 1 文件 ~10 行 |
| P2a | 弗洛伊德示例导入后无字幕（源 JSON 顶层无 subtitles 字段） | 字幕复制为 `samples/FullCutSubtitles.json`；`scripts/import-sample.mjs` 检测并**显式**合入 `payload.subtitles`；**通用 Import API 零改动**（无隐式固定字幕 fallback） | 1 脚本 + 1 样本文件 |
| P2b/c | 项目列表缺 sceneCount/durationSec；composition_id 存了源文件旧值 "FullCutV1" | `api/projects/route.ts` 从 scenes artifact `json_extract` 补两列；import 时 `composition_id` 统一为契约常量 `ZhiyingFullCut` | 2 文件 |

辅助：`scripts/render-frames.mjs`（原 tmp-render-still）bundle 配置与 worker 对齐 + 随机端口 + 帧号参数化，作为帧一致性验证工具保留。

## 二、M1-01 ～ M1-08 判定

| 项 | 判定 | 实证 |
|---|---|---|
| **M1-01** 导入 FullCutScenes.json | **PASS** | `import-sample` POST 201，项目 `8e7cce54-94d3-4cea-9b9b-dcc3784dae52`，85 scenes + 237 字幕入库 |
| **M1-02** Scene 数据无损 | **PASS** | `verify-roundtrip` **682/682 PASS**（最终状态复测）；scenes artifact 与源文件深比较一致；schemaVersion/templateVersion 经 schema 默认值兜底（源文件本身无此二字段） |
| **M1-03** Player 完整预览 | **PASS** | agent-browser 实测：85 场景列表、Player 加载（4 个音频元素挂载）、点击场景 seek 时间码精确（2:13/4:32/6:48）、字幕条渲染正确 |
| **M1-04** Player/Renderer 同构 | **PASS** | 同一 `ZhiyingFullCut` 组件 + 同一 zod schema；**4 个固定帧**（0/3990/8175/12243，覆盖 B-roll/MG/Minimal 三类含字幕帧）pixelmatch diff：**0.02% / 0.47% / 0.68% / 0.44%**，远低于 5% 阈值，差异仅为 970px→1920px 重采样抗锯齿 |
| **M1-05** Job 完整流转 | **PASS** | Job A `4b1d91d8`：queued→running→**succeeded**（5m46s）；Job C `41601473`：stale 恢复后 attempt=2 **succeeded**（9m23s） |
| **M1-06** Worker 六机制 | **PASS** | 单调度串行 ✅ 原子 claim（claimed_by 记录）✅ heartbeat（渲染中 progress 8→22→56→100 持续推进）✅ **stale recovery**（杀 worker → 重启 `recovered 1 stale job` → 重跑成功）✅ **retry**（坏 payload job attempt 1→2 → failed 上限）✅ **cancel**（requestCancel 后 3s 内 cancelled）✅ 优雅退出 requeueJob 函数级验证 PASS |
| **M1-07** MP4 成功输出 | **PASS** | ffprobe：h264 1920×1080@30fps，612.4s（=18372f/30），AAC 音轨，64.2MB；两份产物（fullcut ×2） |
| **M1-08** Jobs 页进度+下载+历史 | **PASS** | agent-browser 实测：四态 Badge/进度条/耗时/错误信息齐全；下载链接 200 + `Content-Disposition: attachment` + 完整字节；dev server 多次重启后历史任务均在（SQLite 持久化） |

## 三、验收任务清单（10 项全完成）

1. ✅ JSX runtime 修复 + 清缓存重打包（renderStill 4 帧成功为证）
2. ✅ retry 端口修复（Job C 同 worker 进程内 retry 渲染全片成功）
3. ✅ 示例字幕导入（237 条），API 无隐式 fallback
4. ✅ sceneCount/durationSec 补齐（列表 API + 首页卡片显示「场景 85 · 时长 10:12」）+ composition_id 统一
5. ✅ 4 帧（>3）Player/Renderer 视觉一致
6. ✅ 完整 Job 走通 succeeded + MP4
7. ✅ Worker heartbeat/stale/retry/cancel/requeue 实测
8. ✅ agent-browser 检查项目列表/Scene Editor/Player/Jobs/下载
9. ✅ round-trip 682/682
10. ✅ `tsc --noEmit` 0 错误 + `next build` 成功

## 四、遗留说明（不阻塞 M1）

- **优雅退出信号路径**：Windows 开发机无法发送真实 SIGTERM（平台限制，taskkill/kill 均为强杀）。`requeueJob` 原子操作已函数级验证 PASS；信号→abort→requeue 代码路径经审查完整，Docker/Linux 部署时由 `docker stop` 生效。
- **Docker 镜像未实机构建**：`next build` 已验证（镜像内同一命令），Dockerfile/compose 与契约逐条对应；部署飞牛时按 `docs/M1_飞牛部署验收清单.md` 验收。
- DB 中保留 4 个测试 job（2 succeeded / 1 cancelled / 1 failed）与 2 份 MP4 作为验收证据。

**M1-01 ～ M1-08 全部 PASS。M1 渲染闭环验收完成。**
