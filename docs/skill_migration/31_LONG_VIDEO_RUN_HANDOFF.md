# 8–12 Minute Long Video Run Handoff

```text
REFERENCE_MASTER = zhiying-video-r3d-reference-v1
TARGET_DURATION = 8–12 minutes
EXECUTION_MODE = current production path
DEFAULT_DELIVERY = clean master + subtitle sidecars
TTS = current NAS IndexTTS2
RENDER = current production Worker / Remotion 4.0.492
MODULE_DECOUPLING = DEFERRED
MAC_RENDER_MIGRATION = DEFERRED
H3 = DEFERRED
```

Start the long-video run in a new session with a new topic. Read the frozen manifest, freeze report, `zhiying-video` Skill, and only the references needed for the selected task.

R3-D is a quality reference, not a scene, palette, template, or subject copy source. Choose the next video's research plan, argument, evidence set, chapter rhythm, and art direction from its own topic.

Keep the current exact-identity SQLite/Worker production path and Remotion `4.0.492`. Use the long run to observe real bottlenecks before proposing module separation, Mac/NAS execution relocation, H3, or a Remotion upgrade.

Do not start the long-video project from this freeze session.
